# Lots of junk goes in here.
/caches/
/backups/

#More junk
probes.json

# Top secret stuff!
/link/.ssh/authorized_keys
/link/.ssh/id_*
/link/.ssh/*known_hosts*
# managed via the SVN script
/link/.ssh/config
/link/.ssh/*key

# Don't commit Vim plugins, vim-plug will install them.
/link/.vim/plugged
/link/.vim/.netrwhist

# Don't commit rbenv temp files.
/link/.rbenv/shims/
/link/.rbenv/versions/
/link/.rbenv/version

# Don't commin any vagrant boxes -> should be a symlink to VM storage anyway
/lib/vagrant/boxes

# Ignore any file with LOCAL in the name (good for per-machine vars/functions)
*LOCAL*

#ignore SSH control-master files
link/.ssh/*master*

#ignore files within link/.config
link/.config/*
#except for midnight commander -- I want this in my repo
!link/.config/mc
